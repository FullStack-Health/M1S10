import { TesterDirective } from './tester.directive';

describe('TesterDirective', () => {
  it('should create an instance', () => {
    const directive = new TesterDirective();
    expect(directive).toBeTruthy();
  });
});
